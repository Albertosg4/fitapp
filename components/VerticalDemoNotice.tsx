'use client'

export default function VerticalDemoNotice() {
  return (
    <div
      style={{
        fontSize: '12px',
        color: '#9ca3af',
        border: '1px dashed rgba(255,255,255,0.2)',
        borderRadius: '10px',
        padding: '8px 10px',
        background: 'rgba(255,255,255,0.02)',
      }}
    >
      Modo demo vertical: no modifica datos, permisos ni configuración real.
    </div>
  )
}
